<view class="tab" style="background-color:#fafafa;width:100%" wx:if="{{id == 1}}">
	<van-tabs sticky color="#18AFFF" bind:change="onChange" class="tab" lazy-render="{{true}}" wx:if="{{id == 1}}">
		<van-tab title="全部" sticky="true" offset-top="0">
			<view class="allBox" wx:if="{{orderInfo.length !== 0 || orderInfoNull.length !== 0}}">
				<view class="allList">
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left" wx:if="{{itemName.type == 1}}">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/smallcjjs.png"></image>
								</view>
								<view class="title_text">城际接送</view>
							</view>
							<view class="title_left" wx:if="{{itemName.type == 2}}">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/smallsckk.png"></image>
								</view>
								<view class="title_text">司乘快客</view>
							</view>
							<view class="title_right" wx:if="{{itemName.status === 10}}">退款中...</view>
							<view class="title_right" style="color:#18AFFF" wx:if="{{itemName.status === 2 }}">待出行</view>
							<view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 1}}">待支付...</view>
							<view class="title_right" style="color:#25BF25" wx:if="{{itemName.status === 6 }}">改签成功</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.start_at}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
									{{itemName.member_number}}人
								</view>
								<view class="start_location">
									<view class="dian"></view>
									{{itemName.from}}
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
									{{itemName.to}}
								</view>
							</view>
							<view class="text_price">
								{{itemName.price_pay}}元
							</view>
						</view>
						<view class="btnBox">
							<text class="go_pay" wx:if="{{itemName.status === 1 &&itemName.type==1}}">支付</text>
							<text class="go_pay" wx:if="{{itemName.status === 1 &&itemName.type==2}}">扫码支付</text>
						</view>
					</view>
					
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfoNull}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left" wx:if="{{itemName.type == 1}}">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/smallcjjs.png"></image>
								</view>
								<view class="title_text">城际接送</view>
							</view>
							<view class="title_left" wx:if="{{itemName.type == 2}}">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/smallsckk.png"></image>
								</view>
								<view class="title_text">司乘快客</view>
							</view>
							<view class="title_right" wx:if="{{itemName.status === 10}}">退款中...</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 5}}">已退款</view>
							<view class="title_right" style="color:#18AFFF" wx:if="{{itemName.status === 2}}">待出行</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 9 || itemName.status === 11}}">已完成</view>
							<view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 1}}">待支付...</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 8}}">已取消...</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.start_at}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx;max-width:400rpx">·</text>
									{{itemName.member_number}}人
								</view>
								<view class="start_location">
									<view class="dian"></view>
									{{itemName.from}}
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
									{{itemName.to}}
								</view>
							</view>

							<view class="text_price">
								{{itemName.price_pay}}元
							</view>
						</view>
					</view>

				</view>

				<view class="no_data" wx:if="noData">已经到底啦</view>
			</view>
			<order-not-find wx:if="{{orderInfo.length == 0 && orderInfoNull.length == 0}}" />
		</van-tab>

		<van-tab title="待支付">
			<view class="allBox" wx:if="{{orderInfo.length !== 0}}">
				<view class="allList">
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left"  wx:if="{{itemName.type == 1}}">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/smallcjjs.png"></image>
								</view>
								<view class="title_text">城际接送</view>
							</view>
							<view class="title_left" wx:if="{{itemName.type == 2}}">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/smallsckk.png"></image>
								</view>
								<view class="title_text">司乘快客</view>
							</view>
							<view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 1}}">待支付...</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.start_at}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
									{{itemName.member_number}}人
								</view>
								<view class="start_location">
									<view class="dian"></view>
									{{itemName.from}}
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
									{{itemName.to}}
								</view>
							</view>

							<view class="text_price">
								{{itemName.price_pay}}元
							</view>
						</view>
						<button class="go_pay" wx:if="{{itemName.status === 1}}">支付</button>
					</view>
				</view>

				<view class="no_data" wx:if="noData">已经到底啦</view>
			</view>
			<order-not-find wx:if="{{orderInfo.length == 0}}" />
		</van-tab>

		<van-tab title="待出行">
			<view class="allBox" wx:if="{{orderInfo.length !== 0}}">
				<view class="allList">

					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left" wx:if="{{itemName.type == 1}}">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/smallcjjs.png"></image>
								</view>
								<view class="title_text">城际接送</view>
							</view>
							<view class="title_left" wx:if="{{itemName.type == 2}}">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/smallsckk.png"></image>
								</view>
								<view class="title_text">司乘快客</view>
							</view>
							<view class="title_right" wx:if="{{itemName.status === 10}}">退款中...</view>
							<view class="title_right" style="color:#18AFFF" wx:if="{{itemName.status === 2}}">待出行</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 4 }}">已完成</view>
							<view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 1}}">待支付...</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 8}}">已取消...</view>
							<view class="title_right" style="color:#25BF25" wx:if="{{itemName.status === 6 }}">改签成功</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.start_at}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
									{{itemName.member_number}}人
								</view>
								<view class="start_location">
									<view class="dian"></view>
									{{itemName.from}}
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
									{{itemName.to}}
								</view>
							</view>

							<view class="text_price">
								{{itemName.price_pay}}元
							</view>
						</view>
						<button class="go_pay" wx:if="{{itemName.status === 1}}">支付</button>
					</view>
				</view>

				<view class="no_data" wx:if="noData">已经到底啦</view>
			</view>
			<order-not-find wx:if="{{orderInfo.length === 0}}" />
		</van-tab>

	</van-tabs>
</view>


<van-tabs sticky color="#18AFFF" bind:change="onChange" class="tab" lazy-render="{{true}}" wx:if="{{id == 2}}">

  <van-tab title="全部">
		<view wx:if="{{init}}">
		  <view class="allBox" wx:if="{{orderInfo.length !== 0 || orderInfoNull.length !== 0}}" >
				<view class="allList">
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/throuhTrain.png"></image>
								</view>
								<view class="title_text">景区直通车</view>
							</view>
							<view class="title_right" style="color:#18AFFF" wx:if="{{itemName.status === 2 }}">待出行</view>
							<view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 1}}">待支付...</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.date}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
									{{itemName.name}}
								</view>
								<view class="start_location">
									<view class="dian"></view>
									海螺沟门票
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
									<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.start_point}}</view>
								</view>
							</view>
							<view class="text_price">
								{{itemName.price}}元
							</view>
						</view>
						<view class="btnBox">
							<text class="go_pay" wx:if="{{itemName.status === 1 }}">支付</text>
						</view>
					</view>
					<!-- 已取消 -->
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfoNull}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/throuhTrain.png"></image>
								</view>
								<view class="title_text">景区直通车</view>
							</view>
							<view class="title_right" wx:if="{{itemName.status === 8}}">退款中...</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 7}}">已退款</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 5 || itemName.status === 6}}">已完成</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 3}}">已取消...</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.date}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx;max-width:400rpx">·</text>
									{{itemName.name}}
								</view>
								<view class="start_location">
									<view class="dian"></view>
									海螺沟门票
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
									<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap;max-width:400rpx">上车点·{{itemName.start_point}}</view>
								</view>
							</view>

							<view class="text_price">
								{{itemName.price}}元
							</view>
						</view>
					</view>
				</view>
				<view class="no_data" wx:if="noData">已经到底啦</view>
			</view>
			<!-- 没有订单信息的组件 -->
			<order-not-find wx:if="{{orderInfo.length == 0 && orderInfoNull.length == 0}}" />
		</view>
  </van-tab>


  <van-tab title="待支付">
    <view wx:if="{{init}}">
        <view class="allBox" wx:if="{{orderInfo.length !== 0}}" >
          <view class="allList">
            <!-- 城际接送 -->
            <view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
              <view class="item_title">
                <view class="title_left">
                  <view class="title_img">
                    <image src="https://static2.jd-gz.com/throuhTrain.png"></image>
                  </view>
                  <view class="title_text">景区直通车</view>
                </view>
                <view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 1}}">待支付...</view>
              </view>
              <view class="item_main">
                <view class="main_text">
                  <view class="text_title">
                    <view class="title_dian"></view>
                    {{itemName.date}}
                    <text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
                    {{itemName.name}}
                  </view>
                  <view class="start_location">
                    <view class="dian"></view>
                    海螺沟门票
                  </view>
                  <view class="shu"></view>
                  <view class="end_location">
                    <view class="dian"></view>
                    <view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap;">上车点·{{itemName.start_point}}</view>
                  </view>
                </view>

                <view class="text_price">
                  {{itemName.price}}元
                </view>
              </view>
              <view class="btnBox">
                <text class="go_pay" wx:if="{{itemName.status === 1 }}">支付</text>
              </view>
            </view>
          </view>

          <view class="no_data" wx:if="noData">已经到底啦</view>
        </view>
        <!-- 没有订单信息的组件 -->
        <order-not-find wx:if="{{orderInfo.length == 0}}" />
    </view>	

    </van-tab>


  <van-tab title="待出行">
		<view wx:if="{{init}}">
			<view class="allBox" wx:if="{{orderInfo.length !== 0}}">
				<view class="allList">
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/throuhTrain.png"></image>
								</view>
								<view class="title_text">景区直通车</view>
							</view>
							<view class="title_right" style="color:#18AFFF" wx:if="{{itemName.status === 2}}">待出行</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.date}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
									{{itemName.name}}
								</view>
								<view class="start_location">
									<view class="dian"></view>
									海螺沟门票
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
									<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap;">上车点·{{itemName.start_point}}</view>
								</view>
							</view>

							<view class="text_price">
								{{itemName.price}}元
							</view>
						</view>
						<view class="btnBox">
							<text class="go_pay" wx:if="{{itemName.status === 1 }}">支付</text>
						</view>
					</view>
				</view>

				<view class="no_data" wx:if="noData">已经到底啦</view>
			</view>
			<!-- 没有订单信息的组件 -->
			<order-not-find wx:if="{{orderInfo.length == 0}}" />	
		</view>
  </van-tab>
</van-tabs>

<van-tabs sticky color="#18AFFF" bind:change="onChange" class="tab" lazy-render="{{true}}" wx:if="{{id == 3}}">

  <van-tab title="全部">
		<view wx:if="{{init}}">
		  <view class="allBox" wx:if="{{orderInfo.length !== 0 || orderInfoNull.length !== 0}}" >
				<view class="allList">
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/scenicIcon.png"></image>
								</view>
								<view class="title_text">景区快客</view>
							</view>
							<view class="title_right" style="color:#18AFFF" wx:if="{{itemName.status === 2 }}">待出行</view>
							<view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 1}}">待支付...</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.date}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
									{{itemName.name}}
								</view>
								<view class="start_location">
									<view class="dian"></view>
									<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.start_point}}</view>
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
									<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">下车点·{{itemName.end_point}}</view>
								</view>
							</view>
							<view class="text_price">
								{{itemName.price}}元
							</view>
						</view>
						<view class="btnBox">
							<text class="go_pay" wx:if="{{itemName.status === 1 }}">支付</text>
						</view>
					</view>
					<!-- 已取消 -->
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfoNull}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/scenicIcon.png"></image>
								</view>
								<view class="title_text">景区快客</view>
							</view>
							<view class="title_right" wx:if="{{itemName.status === 8}}">退款中...</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 7}}">已退款</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 5 || itemName.status === 6}}">已完成</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 3}}">已取消...</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.date}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx;max-width:400rpx">·</text>
									{{itemName.name}}
								</view>
								<view class="start_location">
									<view class="dian"></view>
										<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.start_point}}</view>
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
								<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">下车点·{{itemName.end_point}}</view>
								</view>
							</view>

							<view class="text_price">
								{{itemName.price}}元
							</view>
						</view>
					</view>
				</view>
				<view class="no_data" wx:if="noData">已经到底啦</view>
			</view>
			<!-- 没有订单信息的组件 -->
			<order-not-find wx:if="{{orderInfo.length == 0 && orderInfoNull.length == 0}}" />
		</view>
  </van-tab>


  <van-tab title="待支付">
    <view wx:if="{{init}}">
        <view class="allBox" wx:if="{{orderInfo.length !== 0}}" >
          <view class="allList">
            <!-- 城际接送 -->
            <view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
              <view class="item_title">
                <view class="title_left">
                  <view class="title_img">
                    <image src="https://static2.jd-gz.com/scenicIcon.png"></image>
                  </view>
                  <view class="title_text">景区快客</view>
                </view>
                <view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 1}}">待支付...</view>
              </view>
              <view class="item_main">
                <view class="main_text">
                  <view class="text_title">
                    <view class="title_dian"></view>
                    {{itemName.date}}
                    <text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
                    {{itemName.name}}
                  </view>
                  <view class="start_location">
                    <view class="dian"></view>
                    	<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.start_point}}</view>
                  </view>
                  <view class="shu"></view>
                  <view class="end_location">
                    <view class="dian"></view>
                   <view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">下车点·{{itemName.end_point}}</view>
                  </view>
                </view>

                <view class="text_price">
                  {{itemName.price}}元
                </view>
              </view>
              <view class="btnBox">
                <text class="go_pay" wx:if="{{itemName.status === 1 }}">支付</text>
              </view>
            </view>
          </view>

          <view class="no_data" wx:if="noData">已经到底啦</view>
        </view>
        <!-- 没有订单信息的组件 -->
        <order-not-find wx:if="{{orderInfo.length == 0}}" />
    </view>	

    </van-tab>


  <van-tab title="待出行">
		<view wx:if="{{init}}">
			<view class="allBox" wx:if="{{orderInfo.length !== 0}}">
				<view class="allList">
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/scenicIcon.png"></image>
								</view>
								<view class="title_text">景区快客</view>
							</view>
							<view class="title_right" style="color:#18AFFF" wx:if="{{itemName.status === 2}}">待出行</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.date}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
									{{itemName.name}}
								</view>
								<view class="start_location">
									<view class="dian"></view>
										<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.start_point}}</view>
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
								<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">下车点·{{itemName.end_point}}</view>
								</view>
							</view>

							<view class="text_price">
								{{itemName.price}}元
							</view>
						</view>
						<view class="btnBox">
							<text class="go_pay" wx:if="{{itemName.status === 1 }}">支付</text>
						</view>
					</view>
				</view>

				<view class="no_data" wx:if="noData">已经到底啦</view>
			</view>
			<!-- 没有订单信息的组件 -->
			<order-not-find wx:if="{{orderInfo.length == 0}}" />	
		</view>
  </van-tab>
</van-tabs>


<van-tabs sticky color="#18AFFF" bind:change="onChange" class="tab" lazy-render="{{true}}" wx:if="{{id == 5}}">

  <van-tab title="全部">
		<view wx:if="{{init}}">
		  <view class="allBox" wx:if="{{orderInfo.length !== 0 || orderInfoNull.length !== 0}}" >
				<view class="allList">
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/peripheralTourismIcon.png"></image>
								</view>
								<view class="title_text">周边游</view>
							</view>
							<view class="title_right" style="color:#18AFFF" wx:if="{{itemName.status === 2 }}">待出行</view>
							<view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 1}}">待支付...</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.date}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
									{{itemName.name}}
								</view>
								<view class="start_location">
									<view class="dian"></view>
									<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.location_start}}</view>
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
									<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">下车点·{{itemName.location_end}}</view>
								</view>
							</view>
							<view class="text_price">
								{{itemName.price}}元
							</view>
						</view>
						<view class="btnBox">
							<text class="go_pay" wx:if="{{itemName.status === 1 }}">支付</text>
						</view>
					</view>
					<!-- 已取消 -->
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfoNull}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/peripheralTourismIcon.png"></image>
								</view>
								<view class="title_text">周边游</view>
							</view>
							<view class="title_right" wx:if="{{itemName.status === 8}}">退款中...</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 7}}">已退款</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 5 || itemName.status === 6}}">已完成</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 3}}">已取消...</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.date}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx;max-width:400rpx">·</text>
									{{itemName.name}}
								</view>
								<view class="start_location">
									<view class="dian"></view>
										<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.location_start}}</view>
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
								<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">下车点·{{itemName.location_end}}</view>
								</view>
							</view>

							<view class="text_price">
								{{itemName.price}}元
							</view>
						</view>
					</view>
				</view>
				<view class="no_data" wx:if="noData">已经到底啦</view>
			</view>
			<!-- 没有订单信息的组件 -->
			<order-not-find wx:if="{{orderInfo.length == 0 && orderInfoNull.length == 0}}" />
		</view>
  </van-tab>


  <van-tab title="待支付">
    <view wx:if="{{init}}">
        <view class="allBox" wx:if="{{orderInfo.length !== 0}}" >
          <view class="allList">
            <!-- 城际接送 -->
            <view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
              <view class="item_title">
                <view class="title_left">
                  <view class="title_img">
                    <image src="https://static2.jd-gz.com/peripheralTourismIcon.png"></image>
                  </view>
                  <view class="title_text">周边游</view>
                </view>
                <view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 1}}">待支付...</view>
              </view>
              <view class="item_main">
                <view class="main_text">
                  <view class="text_title">
                    <view class="title_dian"></view>
                    {{itemName.date}}
                    <text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
                    {{itemName.name}}
                  </view>
                  <view class="start_location">
                    <view class="dian"></view>
                    	<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.location_start}}</view>
                  </view>
                  <view class="shu"></view>
                  <view class="end_location">
                    <view class="dian"></view>
                   <view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">下车点·{{itemName.location_end}}</view>
                  </view>
                </view>

                <view class="text_price">
                  {{itemName.price}}元
                </view>
              </view>
              <view class="btnBox">
                <text class="go_pay" wx:if="{{itemName.status === 1 }}">支付</text>
              </view>
            </view>
          </view>

          <view class="no_data" wx:if="noData">已经到底啦</view>
        </view>
        <!-- 没有订单信息的组件 -->
        <order-not-find wx:if="{{orderInfo.length == 0}}" />
    </view>	

    </van-tab>


  <van-tab title="待出行">
		<view wx:if="{{init}}">
			<view class="allBox" wx:if="{{orderInfo.length !== 0}}">
				<view class="allList">
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/peripheralTourismIcon.png"></image>
								</view>
								<view class="title_text">周边游</view>
							</view>
							<view class="title_right" style="color:#18AFFF" wx:if="{{itemName.status === 2}}">待出行</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.date}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
									{{itemName.name}}
								</view>
								<view class="start_location">
									<view class="dian"></view>
										<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.location_start}}</view>
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
								<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">下车点·{{itemName.location_end}}</view>
								</view>
							</view>

							<view class="text_price">
								{{itemName.price}}元
							</view>
						</view>
						<view class="btnBox">
							<text class="go_pay" wx:if="{{itemName.status === 1 }}">支付</text>
						</view>
					</view>
				</view>

				<view class="no_data" wx:if="noData">已经到底啦</view>
			</view>
			<!-- 没有订单信息的组件 -->
			<order-not-find wx:if="{{orderInfo.length == 0}}" />	
		</view>
  </van-tab>
</van-tabs>
<van-tabs sticky color="#18AFFF" bind:change="onChange" class="tab" lazy-render="{{true}}" wx:if="{{id == 6}}">

  <van-tab title="全部">
		<view wx:if="{{init}}">
		  <view class="allBox" wx:if="{{orderInfo.length !== 0 || orderInfoNull.length !== 0}}" >
				<view class="allList">
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/customIcon.png"></image>
								</view>
								<view class="title_text" wx:if="{{itemName.type}}">{{itemName.type}}</view>
								<view class="title_text" wx:if="{{!itemName.type}}">定制游</view>
							</view>
							<view class="title_right" style="color:#18AFFF" wx:if="{{itemName.status === 2 }}">待出行</view>
							<view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 1}}">待支付...</view>
							<view class="title_right" style="color:#18AFFF" wx:if="{{itemName.status === 9}}">进行中</view>
							<view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 10}}">待审核...</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.name}}{{itemName.intro}}		
								</view>
								<view class="start_location">
									<view class="dian"></view>
									<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.location}}</view>
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
									<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">时间·{{itemName.date}}</view>
								</view>
							</view>
							<view class="text_price">
								{{itemName.price}}元
							</view>
						</view>
						<view class="btnBox">
							<text class="go_pay" wx:if="{{itemName.status === 1 }}">支付</text>
						</view>
					</view>
					<!-- 已取消 -->
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfoNull}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/customIcon.png"></image>
								</view>
								<view class="title_text" wx:if="{{itemName.type}}">{{itemName.type}}</view>
								<view class="title_text" wx:if="{{!itemName.type}}">定制游</view>
							</view>
							<view class="title_right" wx:if="{{itemName.status === 8}}">退款中...</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 7}}">已退款</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 5 || itemName.status === 6}}">已完成</view>
							<view class="title_right" style="color:#999999" wx:if="{{itemName.status === 3}}">已取消...</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.intro}}{{itemName.name}}	
								</view>
								<view class="start_location">
									<view class="dian"></view>
										<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.location}}</view>
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
								<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">时间·{{itemName.date}}</view>
								</view>
							</view>

							<view class="text_price">
								{{itemName.price}}元
							</view>
						</view>
					</view>
				</view>
				<view class="no_data" wx:if="noData">已经到底啦</view>
			</view>
			<!-- 没有订单信息的组件 -->
			<order-not-find wx:if="{{orderInfo.length == 0 && orderInfoNull.length == 0}}" />
		</view>
  </van-tab>


  <van-tab title="待支付">
    <view wx:if="{{init}}">
        <view class="allBox" wx:if="{{orderInfo.length !== 0}}" >
          <view class="allList">
            <!-- 城际接送 -->
            <view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
              <view class="item_title">
                <view class="title_left">
                  <view class="title_img">
                    <image src="https://static2.jd-gz.com/customIcon.png"></image>
                  </view>
                  <view class="title_text" wx:if="{{itemName.type}}">{{itemName.type}}</view>
								<view class="title_text" wx:if="{{!itemName.type}}">定制游</view>
                </view>
                <view class="title_right" style="color:#FE4B3B" wx:if="{{itemName.status === 1}}">待支付...</view>
              </view>
              <view class="item_main">
                <view class="main_text">
                  <view class="text_title">
                    <view class="title_dian"></view>
                    {{itemName.name}}{{itemName.intro}}
                  </view>
                  <view class="start_location">
                    <view class="dian"></view>
                    	<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.location}}</view>
                  </view>
                  <view class="shu"></view>
                  <view class="end_location">
                    <view class="dian"></view>
                   <view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">时间·{{itemName.date}}</view>
                  </view>
                </view>

                <view class="text_price">
                  {{itemName.price}}元
                </view>
              </view>
              <view class="btnBox">
                <text class="go_pay" wx:if="{{itemName.status === 1 }}">支付</text>
              </view>
            </view>
          </view>

          <view class="no_data" wx:if="noData">已经到底啦</view>
        </view>
        <!-- 没有订单信息的组件 -->
        <order-not-find wx:if="{{orderInfo.length == 0}}" />
    </view>	

    </van-tab>


  <van-tab title="待出行">
		<view wx:if="{{init}}">
			<view class="allBox" wx:if="{{orderInfo.length !== 0}}">
				<view class="allList">
					<view class="list_item" catchtap="toOrderDetail" wx:for="{{orderInfo}}" wx:key="*this" wx:for-item="itemName" data-id='{{itemName}}'>
						<view class="item_title">
							<view class="title_left">
								<view class="title_img">
									<image src="https://static2.jd-gz.com/customIcon.png"></image>
								</view>
							<view class="title_text" wx:if="{{itemName.type}}">{{itemName.type}}</view>
								<view class="title_text" wx:if="{{!itemName.type}}">定制游</view>
							</view>
							<view class="title_right" style="color:#18AFFF" wx:if="{{itemName.status === 2}}">待出行</view>
						</view>
						<view class="item_main">
							<view class="main_text">
								<view class="text_title">
									<view class="title_dian"></view>
									{{itemName.date}}
									<text style="color:#D3D3D3;margin-left:10rpx;margin-right:10rpx;font-size:36rpx">·</text>
									{{itemName.name}}
								</view>
								<view class="start_location">
									<view class="dian"></view>
										<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">上车点·{{itemName.location}}</view>
								</view>
								<view class="shu"></view>
								<view class="end_location">
									<view class="dian"></view>
								<view style="overflow: hidden;text-overflow: ellipsis;white-space: nowrap; max-width:400rpx">时间·{{itemName.date}}</view>
								</view>
							</view>

							<view class="text_price">
								{{itemName.price}}元
							</view>
						</view>
						<view class="btnBox">
							<text class="go_pay" wx:if="{{itemName.status === 1 }}">支付</text>
						</view>
					</view>
				</view>

				<view class="no_data" wx:if="noData">已经到底啦</view>
			</view>
			<!-- 没有订单信息的组件 -->
			<order-not-find wx:if="{{orderInfo.length == 0}}" />	
		</view>
  </van-tab>
</van-tabs>
<!-- 没有订单信息的组件 -->
<order-not-find wx:if="{{id == 4}}" />